[project]
name = "law-rag-chatbot"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "hf-transfer>=0.1.9",
    "huggingface-hub>=1.22.0",
]
